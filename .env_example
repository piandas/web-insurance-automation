# ==========================================
# CONFIGURACIÓN SENSIBLE (Variables de entorno) RENOMBRA ESTE ARCHIVO A .env
# ==========================================

# Archivo .env para credenciales y configuración sensible
USUARIO=ABC123456789
CONTRASENA=ABC123456789

# HEADLESS controla si el navegador se muestra (False) o corre en segundo plano (True)
HEADLESS=False

# URL base del sistema
BASE_URL=https://www.allia2net.com.co

# Timeout en milisegundos de espera para las solicitudes
# Aumenta este valor si se tienen problemas de conexión o lentitud
TIMEOUT=30000

# ==========================================
# NOTA: Las demás configuraciones como placa, 
# documentos, ciudades, etc. están en config.py
# ya que no son datos sensibles.
# ==========================================
